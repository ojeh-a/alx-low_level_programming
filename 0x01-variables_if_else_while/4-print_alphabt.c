#include <stdio.h>

/**
 * main - print alphabets
 *
 * Return: return 0
 */
int main(void)
{
	char alpha = 'a';
	char que = 'q';
	char era = 'e';
	
	if ('alpha' <= 'z';)
	{
		putchar(alpha);
		alpha++;
		while (alpha != era || que);
	}
	return (0);
}
