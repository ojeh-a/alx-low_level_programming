#include <stdio.h>

/**
 * main - print alphabets
 *
 * Return: return 0
 */
int main(void)
{
	int alpha = 'a';
	int que = 'q';
	int era = 'e';
	
	if (alpha <= 'z'; alpha != que || era; alpha++)
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
