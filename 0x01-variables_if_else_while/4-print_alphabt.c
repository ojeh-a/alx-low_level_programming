#include <stdio.h>

/**
 * main - print alphabets
 *
 * Return: return 0
 */
int main(void)
{
	char alpha = 'a';
	char alphaz = 'z';
	char q = 'q';
	char e = 'e';

	for ( alpha <= alphaz && != e, q; alpha++)
	{
		putchar(alpha);
	}
	return (0)
}
