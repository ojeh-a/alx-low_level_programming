#include <stdio.h>

/**
 * main - print alphabets
 *
 * Return: return 0
 */
int main(void)
{
	int alpha = 'a';
	
	if (alpha <= 'z' && alpha != 'e' || 'q'; alpha++)
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
