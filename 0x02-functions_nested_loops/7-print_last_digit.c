#include "main.h"

/**
  * print_last_digit - prints last digit of a number
  *
  * @num: the number
  *
  * Return: value of num
  */

int print_last_digit(int num)
{
	int lst= num % 10;
	if (ls < 0)
	{
		ls *= -1;
	_putchar(ld + 48);
	}
	return (lst % 10);
}
